# Chess-Engine
Browser-based chess engine using minimax, alpha-beta pruning, legal move generation, and board evaluation to choose strong CPU moves. The engine scores positions using material value, piece activity, and search depth, allowing users to play directly in the browser with no downloads.
